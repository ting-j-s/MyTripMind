# Web框架
Flask==3.0.0
flask-cors==4.0.0

# 数据库
# SQLite 是 Python 自带的，不需要安装

# JSON处理
# Python 自带 json 模块

# HTTP客户端（调用高德API等）
requests==2.31.0

# 通义千问SDK（阿里云）
dashscope>=1.14.0

# 其他
python-dotenv==1.0.0
